# ~/.ssh/config

# cl
Host                *.cl.int
IdentityFile        ~/.ssh/cl_id_rsa
ForwardAgent        yes

Host                humilis
HostName            192.241.200.171
ForwardAgent        yes

# forward ssh keys in local network
Host                192.168.0.*
ForwardAgent        yes

# don't trust external machines with ssh keys
Host                *
ForwardAgent        no
Protocol            2,1
